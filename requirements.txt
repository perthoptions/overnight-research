pandas>=2.0
numpy>=1.24
pyarrow>=14.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
jupyter>=1.0

# IBKR — ib_async needs Python 3.10+; ib_insync is the 3.9-compatible predecessor
ib_async>=1.0 ; python_version >= "3.10"
ib_insync>=0.9.86 ; python_version < "3.10"

# Bloomberg BQuant notebooks require `bql`, which is only available inside BQuant
# and cannot be installed from PyPI.
